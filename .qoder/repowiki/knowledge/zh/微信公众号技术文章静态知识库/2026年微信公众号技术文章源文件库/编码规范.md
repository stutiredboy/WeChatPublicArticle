- 每篇文章独立一个目录，目录名即为文章标题（含中文标点），主文件与目录同名且后缀为 `.md`。
- 文章内嵌图片统一存放在同级 `images/` 目录，引用时使用相对路径 `![](images/文件名.扩展名)`。
- Markdown 文件顶部使用 YAML front matter 声明 `title` 字段，便于后续索引或批量处理。
- 需要额外产物的文章（HTML/PDF/ZIP）在文章目录下集中存放，并通过独立的 Python 脚本进行转换或打包。