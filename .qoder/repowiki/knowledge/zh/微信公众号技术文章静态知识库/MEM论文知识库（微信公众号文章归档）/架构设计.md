纯静态内容仓库，采用扁平化的目录结构：根目录 mem/ 下按年份（如 2026/）和月份（如 02/、03/、05/、06/、07/）两级子目录组织；每个文章独立占一个以中文标题命名的文件夹，内部包含一篇同名 .md 文件及一个 images/ 子目录存放配图。每篇文章 Markdown 头部使用 YAML front matter 声明 title、date、source（微信公众号链接）、images 等元数据，正文通过 ![](images/xxx.jpg) 引用本地图片。模块内无代码、无构建脚本，仅作为内容源供上层站点或导出流程消费。